# TRSC updates

This repository is for panel updates only. Source code is not published.

Put new builds in GitHub Releases:

- `trsc-windows-amd64.exe`
- `trsc-linux-amd64`

`version.json` is the fallback version file for the panel updater.
